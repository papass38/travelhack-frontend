import { Button, StyleSheet, Text, View } from 'react-native';

export default function ToDoScreen() {
    return (
        <View style={styles.container}>
            <Text>to do page?</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });